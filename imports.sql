CREATE TABLE book_ids (
  bookID text
);

CREATE TABLE books (
  bookID text,
  title text,
  author text,
  rank integer,
  status text,
  review text,
  tag text,
  memo text,
  read_at
);
